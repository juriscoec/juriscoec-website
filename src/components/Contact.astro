---
import Typography from "./Typography.astro";

interface ContactInfo {
  icon: string;
  title: string;
  content: string;
  link?: string;
}

const contactInfo: ContactInfo[] = [
  {
    icon: "📍",
    title: "Dirección",
    content: "Av. Principal 123, Edificio Legal, Piso 15",
    link: "https://maps.google.com"
  },
  {
    icon: "📞",
    title: "Teléfono",
    content: "+593 (2) 234-5678",
    link: "tel:+59322345678"
  },
  {
    icon: "✉️",
    title: "Email",
    content: "contacto@bufeteabogados.com",
    link: "mailto:contacto@bufeteabogados.com"
  },
  {
    icon: "⏰",
    title: "Horario",
    content: "Lunes a Viernes: 8:00 AM - 6:00 PM"
  }
];
---

<section id="contacto" class="relative min-h-screen flex items-center py-24">
  <picture class="absolute inset-0 -z-10">
    <source
      media="(min-width: 768px)"
      srcset="/images/contact-bg.jpg"
    />
    <img
      src="/images/contact-bg-mobile.jpg"
      alt="Fondo de contacto"
      class="w-full h-full object-cover"
    />
  </picture>

  <div class="absolute inset-0 bg-black/75 -z-10"></div>

  <div class="container mx-auto px-4">
    <div class="max-w-7xl mx-auto">
      <div class="grid lg:grid-cols-2 gap-12">
        <div class="space-y-8">
          <div>
            <h2 class="text-4xl font-bold text-gradient-primary mb-4">
              <Typography>Contáctenos</Typography>
            </h2>
            <p class="text-xl text-light leading-relaxed">
              Estamos aquí para ayudarte. Ponte en contacto con nosotros y te responderemos a la brevedad posible.
            </p>
          </div>

          <div class="grid sm:grid-cols-2 gap-6">
            {
              contactInfo.map(info => (
                <div class="bg-light/10 backdrop-blur rounded-lg p-6 hover:bg-light/15 transition-colors">
                  <div class="flex items-start gap-4">
                    <span class="text-2xl">{info.icon}</span>
                    <div>
                      <h3 class="text-light font-semibold mb-2">{info.title}</h3>
                      {info.link ? (
                        <a
                          href={info.link}
                          class="text-light/80 hover:text-primary transition-colors"
                          target={info.link.startsWith('http') ? '_blank' : undefined}
                          rel={info.link.startsWith('http') ? 'noopener noreferrer' : undefined}
                        >
                          {info.content}
                        </a>
                      ) : (
                        <p class="text-light/80">{info.content}</p>
                      )}
                    </div>
                  </div>
                </div>
              ))
            }
          </div>
        </div>

        <!-- Formulario de contacto -->
        <div class="bg-light/10 backdrop-blur rounded-xl p-8">
          <form class="space-y-6">
            <div class="grid sm:grid-cols-2 gap-6">
              <div>
                <label for="nombre" class="block text-light mb-2">Nombre</label>
                <input
                  type="text"
                  id="nombre"
                  name="nombre"
                  class="w-full px-4 py-3 rounded-lg bg-light/5 border border-light/10 text-light placeholder:text-light/50 focus:outline-none focus:border-primary"
                  placeholder="Tu nombre"
                  required
                />
              </div>
              <div>
                <label for="email" class="block text-light mb-2">Email</label>
                <input
                  type="email"
                  id="email"
                  name="email"
                  class="w-full px-4 py-3 rounded-lg bg-light/5 border border-light/10 text-light placeholder:text-light/50 focus:outline-none focus:border-primary"
                  placeholder="tu@email.com"
                  required
                />
              </div>
            </div>

            <div>
              <label for="asunto" class="block text-light mb-2">Asunto</label>
              <input
                type="text"
                id="asunto"
                name="asunto"
                class="w-full px-4 py-3 rounded-lg bg-light/5 border border-light/10 text-light placeholder:text-light/50 focus:outline-none focus:border-primary"
                placeholder="Asunto de tu mensaje"
                required
              />
            </div>

            <div>
              <label for="mensaje" class="block text-light mb-2">Mensaje</label>
              <textarea
                id="mensaje"
                name="mensaje"
                rows="4"
                class="w-full px-4 py-3 rounded-lg bg-light/5 border border-light/10 text-light placeholder:text-light/50 focus:outline-none focus:border-primary resize-none"
                placeholder="Escribe tu mensaje aquí..."
                required
              ></textarea>
            </div>

            <button
              type="submit"
              class="w-full bg-primary text-light font-semibold py-3 px-6 rounded-lg hover:bg-primary-dark transition-colors"
            >
              Enviar Mensaje
            </button>
          </form>
        </div>
      </div>
    </div>
  </div>
</section>